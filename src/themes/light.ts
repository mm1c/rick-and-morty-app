import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontSize: 14,
  },
  palette: {
    background: {
      default: "#fff",
    },
    text: {
      primary: "#000",
    },
  },
});

export default theme;